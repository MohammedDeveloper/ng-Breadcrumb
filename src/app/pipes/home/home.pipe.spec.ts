/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HomePipe } from './home.pipe';

describe('HomePipe', () => {
  it('create an instance', () => {
    const pipe = new HomePipe();
    expect(pipe).toBeTruthy();
  });
});
